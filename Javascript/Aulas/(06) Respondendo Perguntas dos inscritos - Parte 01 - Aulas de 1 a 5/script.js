// (06) Respondendo Perguntas dos inscritos - Parte 01 - Aulas de 1 a 5

/*
   Diferença entre == e ===
let n1 = 1
let n2 = '1'


// console.log(n1 == n2) // Verifica o VALOR das variaveis TRUE
// console.log(n1 === n2) // Verifica o TIPO das variaveis FALSE

let v1 = { nome: 'Kaido' }
let v2 = { nome: 'Kaido' }
console.log(v1 == v2) // False
console.log(v1 === v2) // False porque ele também verifica o endereço de memoria*/

/* 
   // Entrada de Dados: Necessita de interface grafica (HTML)
let nome = prompt('Digite seu nome')
console.log(nome) */

/* // Operador !=
let n1 = 1
let n2 = 2
console.log(n1 != n2) */

/* // Identificar onde o código está rodando com JS
if(
   navigator.userAgent.match(/Android/i)
   || navigator.userAgent.match(/webOS/i)
   || navigator.userAgent.match(/iPhone|iPad|iPod/i)
   || navigator.userAgent.match(/BlackBerry/i)
   || navigator.userAgent.match(/Windows Phone/i)
   || navigator.userAgent.match(/Opera Mini/i)
   || navigator.userAgent.match(/Opera IEMobile/i)
) {
   console.log('Celular')
} else {
   console.log('PC')
} */

